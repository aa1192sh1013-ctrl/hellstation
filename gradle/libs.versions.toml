# 버전 카탈로그.
# 라이브러리 버전은 전부 여기서만 바꿉니다. build.gradle.kts에 숫자를 직접 쓰지 마세요.
# 2026-08-22 기준 안정 버전으로 맞춰져 있습니다.
#
# ## 버전을 고를 때 확인해야 하는 것
#
# 아티팩트가 Maven 에 "존재하는지"만 보면 안 됩니다. 각 AAR 안에는
# `aar-metadata.properties` 가 들어 있고 거기 minCompileSdk 가 적혀 있습니다.
# AGP 가 checkAarMetadata 단계에서 그 값을 검사합니다.
#
# 처음에 최신 버전으로만 맞췄더니 Compose 1.12.0 · core-ktx 1.19.0 ·
# lifecycle 2.11.0 · okhttp 5.5.0 이 전부 **compileSdk 37** 을 요구해서
# 31건이 걸렸습니다. 그런데 `platforms/android-37` 은 아직 정식 채널에 없습니다
# ("Package platforms/android-37 not found").
#
# 그래서 정식 최신 플랫폼인 36 에 맞는 버전으로 내렸습니다.
# android-37 이 정식 공개되면 위 네 개와 AGP 9.x 로 함께 올리면 됩니다.

[versions]
agp = "8.13.2"
kotlin = "2.4.10"

compileSdk = "36"
minSdk = "26"          # java.time을 desugaring 없이 쓰기 위한 하한 (docs/data-model.md 참고)
targetSdk = "36"

coreKtx = "1.18.0"      # 1.19.0 은 compileSdk 37 필요
lifecycle = "2.10.0"    # 2.11.0 은 compileSdk 37 필요
activityCompose = "1.13.0"
composeBom = "2026.06.01"  # 2026.08.00 은 Compose 1.12.0 -> compileSdk 37 필요
navigationCompose = "2.9.8"
splashscreen = "1.2.0"

coroutines = "1.11.0"
serializationJson = "1.11.0"
okhttp = "5.4.0"        # 5.5.0 은 compileSdk 37 필요
retrofit = "2.12.0"
retrofitSerialization = "1.0.0"
datastore = "1.2.1"

junit = "4.13.2"
androidxJunit = "1.3.0"
espresso = "3.7.0"

[libraries]
# --- AndroidX 기본 ---
androidx-core-ktx = { group = "androidx.core", name = "core-ktx", version.ref = "coreKtx" }
androidx-core-splashscreen = { group = "androidx.core", name = "core-splashscreen", version.ref = "splashscreen" }
androidx-lifecycle-runtime-ktx = { group = "androidx.lifecycle", name = "lifecycle-runtime-ktx", version.ref = "lifecycle" }
androidx-lifecycle-runtime-compose = { group = "androidx.lifecycle", name = "lifecycle-runtime-compose", version.ref = "lifecycle" }
androidx-lifecycle-viewmodel-compose = { group = "androidx.lifecycle", name = "lifecycle-viewmodel-compose", version.ref = "lifecycle" }

# --- Compose ---
androidx-compose-bom = { group = "androidx.compose", name = "compose-bom", version.ref = "composeBom" }
androidx-activity-compose = { group = "androidx.activity", name = "activity-compose", version.ref = "activityCompose" }
androidx-compose-ui = { group = "androidx.compose.ui", name = "ui" }
androidx-compose-ui-graphics = { group = "androidx.compose.ui", name = "ui-graphics" }
androidx-compose-ui-tooling = { group = "androidx.compose.ui", name = "ui-tooling" }
androidx-compose-ui-tooling-preview = { group = "androidx.compose.ui", name = "ui-tooling-preview" }
androidx-compose-ui-test-manifest = { group = "androidx.compose.ui", name = "ui-test-manifest" }
androidx-compose-ui-test-junit4 = { group = "androidx.compose.ui", name = "ui-test-junit4" }
androidx-compose-material3 = { group = "androidx.compose.material3", name = "material3" }
androidx-compose-material-icons-extended = { group = "androidx.compose.material", name = "material-icons-extended" }
androidx-navigation-compose = { group = "androidx.navigation", name = "navigation-compose", version.ref = "navigationCompose" }

# --- 데이터 (데이터·기능 담당용. 지금은 쓰이지 않아도 미리 넣어 둡니다) ---
kotlinx-coroutines-android = { group = "org.jetbrains.kotlinx", name = "kotlinx-coroutines-android", version.ref = "coroutines" }
kotlinx-serialization-json = { group = "org.jetbrains.kotlinx", name = "kotlinx-serialization-json", version.ref = "serializationJson" }
okhttp = { group = "com.squareup.okhttp3", name = "okhttp", version.ref = "okhttp" }
okhttp-logging = { group = "com.squareup.okhttp3", name = "logging-interceptor", version.ref = "okhttp" }
retrofit = { group = "com.squareup.retrofit2", name = "retrofit", version.ref = "retrofit" }
retrofit-serialization = { group = "com.jakewharton.retrofit", name = "retrofit2-kotlinx-serialization-converter", version.ref = "retrofitSerialization" }
androidx-datastore-preferences = { group = "androidx.datastore", name = "datastore-preferences", version.ref = "datastore" }

# --- 테스트 ---
junit = { group = "junit", name = "junit", version.ref = "junit" }
kotlinx-coroutines-test = { group = "org.jetbrains.kotlinx", name = "kotlinx-coroutines-test", version.ref = "coroutines" }
androidx-junit = { group = "androidx.test.ext", name = "junit", version.ref = "androidxJunit" }
androidx-espresso-core = { group = "androidx.test.espresso", name = "espresso-core", version.ref = "espresso" }

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }
kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }
kotlin-compose = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }
kotlin-serialization = { id = "org.jetbrains.kotlin.plugin.serialization", version.ref = "kotlin" }
