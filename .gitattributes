# 줄바꿈을 저장소 안에서는 LF로 통일합니다.
# 윈도우에서 작업해도 macOS·리눅스에서 그대로 동작하게 하려는 것입니다.
* text=auto eol=lf

# 윈도우 배치 파일은 CRLF여야 합니다.
*.bat  text eol=crlf
*.cmd  text eol=crlf

# gradlew 는 CRLF가 되면 리눅스에서 실행 자체가 안 됩니다.
gradlew text eol=lf

# 바이너리는 건드리지 않습니다.
*.png  binary
*.jpg  binary
*.jar  binary
*.apk  binary
