<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HellStation"

        android:networkSecurityConfig="@xml/network_security_config">
        <!--
            평문 HTTP 허용 범위를 좁혔습니다. (화면 담당)

            전에는 usesCleartextTraffic="true" 로 앱 전체 통신이 열려 있었습니다.
            지금은 res/xml/network_security_config.xml 이 서울 열린데이터광장 두 도메인만 열고
            나머지는 전부 막습니다. 새 API가 HTTPS를 쓴다면 손댈 필요가 없습니다.

            아이콘과 테마도 앱 것으로 바뀌었습니다.
            - 아이콘: res/mipmap-anydpi-v26/ic_launcher.xml (지하철 악마 캐릭터)
            - 테마:   res/values/themes.xml, res/values-night/themes.xml
        -->

        <activity
            android:name=".navigation.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
